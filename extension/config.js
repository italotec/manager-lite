// Preenchido pelo Manager Lite ao gerar o ZIP de download (app/routes/extension_bp.py).
// Os valores abaixo são apenas placeholders para desenvolvimento local.
//
// `self` (não `window`) — este arquivo é carregado via importScripts() dentro do service worker
// (background.js), onde `window` não existe. Em qualquer contexto com documento, self === window,
// então isso também funciona se algum dia for carregado numa página.
self.__ML_CONFIG__ = {
  apiKey: "",
  baseUrl: "http://localhost:5012"
};
